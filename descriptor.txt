Problem Descriptor JSON
------------------------

problemType:
	intAddition:
		# carries
		# digits per operator
	fracAddition:
		# denominators to convert (in [0,1,2])
		# sum > 1 (true or false)
	intSubtration:
		# borrows
		# digits per operator (array)
		has borrow across zero (true or false)
		difference can be negative (true or false)
	fracSubtration:
		# denominators to convert (in [0,1,2])
		difference can be negative (true or false)
	intMultiplication:
		# digits per operator (array)
	intDivision:
		# digits per operator (array)
		has remainder (true or false)

----------------------------------------------------------

Operator:
	+:
		number type:
			integer:
				# carries
				# digits per operator (array)
			fraction:
				# denominators to convert (in [0,1,2])
				# sum > 1 (true or false)
				
	-:
		number type:
			integer:
				# borrows
				# digits per operator (array)
				has borrow across zero (true or false)
			fraction:
				# denominators to convert (in [0,1,2])
		difference can be negative (true or false)
			
	*:
		number type:
			integer:
				# digits per operator (array)
				
	/:
		# digits per operator (array)
		has remainder (true or false)


How to parse the input:
-----------------------









